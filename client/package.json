{
  "name": "client",
  "scripts": {
    "start": "snowpack dev",
    "test": "echo Error: no test specified && exit 1"
  },
  "dependencies": {
    "snowpack": "^3.8.8",
    "socket.io-client": "^4.5.4"
  }
}
